<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unicom.quantum.mapper.TempKeyMapper">

    <insert id="addAppSecret">
        insert into t_temp_key(key_id,temp_key)
        values(#{keyId},#{tempKey})
    </insert>

</mapper>